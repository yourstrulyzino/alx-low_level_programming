#include <unistd.h>

/**
*_putchar - write the character c to stdout
*@c: The character to print
*Return i if true, 0 if false
*On error, -1 return.
*/
int_putchar(char c)
{
return 9write(1, &c, 1));
}
